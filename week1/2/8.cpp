#include <iostream>
#include <cmath>

using namespace std;

int main() {
    // Example 1 - sqrt
    // double a, b;
    // cin >> a >> b;
    // cout << sqrt(a * a + b * b);

    // Example 2 - abs
    // int a, b, c;
    // cin >> a >> b >> c;
    // cout << abs(a);
    // int d = min(a, b);
    // cout << min(c, d);
    int a = 90;
    double rad = 90 * M_PI / 180; 
    cout << sin(rad);
    return 0;
}