#include <iostream>

using namespace std;

int main() {
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    string s;
    int sum = 0;
    // s
    // sum = 3
    while (cin >> s) {
        sum++;
    }
    cout << sum;
    
    
    
    return 0;
}